{
  "name": "sucataapp",
  "version": "1.0.0",
  "description": "Gestão de sucata — PWA com Node.js + PostgreSQL",
  "scripts": {
    "start": "node backend/server.js",
    "postinstall": "cd backend && npm install --omit=dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
